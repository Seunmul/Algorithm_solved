#include <bits/stdc++.h>
using namespace std;
int main() {
    int A,B;
    ios::sync_with_stdio(false);
	cin.tie(NULL);
    while(cin >> A >> B and (A!=0 and B!=0))
        cout<<A+B<<"\n";
    return 0;
}