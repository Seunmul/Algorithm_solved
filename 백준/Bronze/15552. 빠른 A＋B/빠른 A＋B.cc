#include <bits/stdc++.h>
int T, A, B;
using namespace std;
int main()
{
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    cin >> T;
    while (T--){
        cin >> A >> B; cout << A + B<<"\n"; 
    }
    return 0;
}